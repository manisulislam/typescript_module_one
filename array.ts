const superheros: string[]=[]
const heroPower: Array<number>=[]

type User={
    name: string;
    isActive: boolean;
}

const allUser: User[]=[];

superheros.push("anis");
heroPower.push(1);
allUser.push({name: "", isActive: true})
