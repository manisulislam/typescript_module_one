const user={
    name: "anis",
    email: "anis@gamil.com",
    isActive: true,
}
function createUser({name: string, isPaid: boolean}){

}
createUser({name: "anis", isPaid: false});
function createCourse():{name: string, price: number}{
    return {name:"next.js", price: 562}
}
export {};